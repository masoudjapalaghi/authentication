in _app.js
// «useSession()» در هر نقطه از برنامه شما برای دسترسی به شی «session».
// گزینه های ارائه دهنده مورد نیاز نیستند اما می توانند در شرایطی مفید باشند
       // شما حداکثر زمان جلسه کوتاهی دارید. در اینجا با مقادیر پیش فرض نشان داده شده است.
       گزینه ها={{
         // Stale Time کنترل می کند که هر چند وقت یکبار useSession در کلاینت باید انجام شود
         // برای همگام سازی وضعیت جلسه با سرور تماس بگیرید. ارزش در ثانیه
         // به عنوان مثال.
         // * 0 - غیرفعال (همیشه از مقدار کش استفاده کنید)
         // * 60 - همگام سازی حالت جلسه با سرور اگر قدیمی تر از 60 ثانیه باشد
         staleTime: 0،
         // بازه زمانی بازیابی به پنجره‌ها / برگه‌هایی که وارد سیستم شده‌اند می‌گوید به ارسال ادامه دهند
         // یک درخواست نگه داشتن زنده (که انقضای جلسه جاری را تمدید می کند) به
         // از منقضی شدن جلسات در پنجره های باز جلوگیری کنید. ارزش در ثانیه
         //
         // توجه: اگر یک جلسه منقضی شده است زمانی که نگه داشتن زنده فعال می شود، همه باز هستند
         // پنجره ها / برگه ها به روز می شوند تا نشان دهند کاربر از سیستم خارج شده است.
         بازه زمانی: 0

in [...nextAuth].js
Credentials
/ نامی که باید روی علامت در فرم نمایش داده شود (مثلاً "ورود به سیستم با...")
       // اعتبار برای ایجاد یک فرم مناسب در صفحه ورود استفاده می شود.
       // می توانید هر فیلدی را که انتظار دارید ارسال شود مشخص کنید.
       // به عنوان مثال. دامنه، نام کاربری، رمز عبور، توکن 2FA و غیره
       // می توانید هر ویژگی HTML را از طریق شی به تگ <input> منتقل کنید.